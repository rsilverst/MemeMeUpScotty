# Meme Me Up Scotty — project instructions

All project instructions live in [`AGENTS.md`](AGENTS.md), the single source of truth shared by
every AI coding tool. This file exists only so Claude Code auto-loads them — do not add rules
here; add them to `AGENTS.md`.

@AGENTS.md
